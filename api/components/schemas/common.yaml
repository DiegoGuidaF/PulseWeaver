ErrorResponse:
  type: object
  properties:
    error:
      type: string
      example: device not found

ID:
  type: integer
  format: int64

PolicyDenyReason:
  type: string
  description: >
    Why the policy engine denied an access request. The complete set of values
    that can appear in an access log entry's deny_reason or be used as the
    deny_reason filter.
  enum:
    - no_device_match
    - ip_not_registered
    - invalid_token
    - host_not_allowed

IPAddress:
  type: string
  description: "IPv4 or IPv6 address"
  anyOf:
    - type: string
      format: ipv4
    - type: string
      format: ipv6
  example: "192.168.1.132"

GeoInfo:
  type: object
  description: >
    GeoIP-derived metadata for an IP, resolved on read. Every field is optional:
    individual fields are absent when the database has no value for that IP, and
    the whole object is absent (null) when the IP is unresolvable (private range,
    unknown) or GeoIP enrichment is disabled.
  properties:
    country_code:
      type: string
      description: "ISO 3166-1 alpha-2 country code, e.g. \"DE\"."
      example: "DE"
    country_name:
      type: string
      description: "Country name in English, e.g. \"Germany\"."
      example: "Germany"
    continent_code:
      type: string
      description: "Continent code, e.g. \"EU\"."
      example: "EU"
    asn:
      type: integer
      format: int64
      description: "Autonomous System Number, e.g. 13335."
      example: 13335
    asn_org:
      type: string
      description: "Autonomous System organization, e.g. \"Cloudflare, Inc.\"."
      example: "Cloudflare, Inc."

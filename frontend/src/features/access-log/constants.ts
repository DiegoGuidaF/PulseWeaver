export { DATEPICKER_TIME_PRESETS, DEFAULT_PRESET_KEY, PRESET_MS } from "@/lib/timePresets";
export type { PresetKey } from "@/lib/timePresets";

export const DENY_REASON_LABELS: Record<string, string> = {
    no_device_match: "No matching device",
    ip_not_registered: "IP not in any device or network policy",
    invalid_token: "Invalid token",
};

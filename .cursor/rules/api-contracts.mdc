---
globs:
  - "api/openapi.yaml"
  - "frontend/src/lib/api/**"
  - "**/server.gen.go"
---

# API Contracts & Type Generation Rules

## Schema-First API Design

- **Single Source of Truth:** `api/openapi.yaml` (OpenAPI 3.0.1)
- **No Manual Types:** All API types are generated from the OpenAPI spec
- **Backend Types:** Generated via `oapi-codegen` → `internal/api/server.gen.go`
- **Frontend Types:** Generated via `@hey-api/openapi-ts` → `frontend/src/lib/api/` (multiple files: `index.ts`, `sdk.gen.ts`, `types.gen.ts`, `zod.gen.ts`, etc.)

## ⚠️ Generated API Directory — Do Not Modify

**The entire `frontend/src/lib/api/` directory is auto-generated by `@hey-api/openapi-ts`.**
- **NEVER** modify, add, or delete any file inside `frontend/src/lib/api/`.
- All changes there are overwritten on API regeneration.
- Custom API wiring (config, error helpers, query keys) lives in `frontend/src/lib/api-client/`.

## OpenAPI Specification Rules

### Structure
- Use OpenAPI 3.0.1
- Define all endpoints, request/response schemas, and error responses
- Use `$ref` for reusable schema components
- Include proper security definitions (cookieAuth, bearerAuth)

### Schema Components
- Define reusable schemas in `components/schemas/`
- Use descriptive names (e.g., `CreateUserRequest`, `Device`, `ErrorResponse`)
- Include validation rules in schema (minLength, maxLength, pattern, etc.)

### Endpoint Definition
- Use `operationId` for each endpoint (used for code generation)
- Include proper tags for grouping
- Document all possible response codes (200, 400, 401, 403, 404, 409, 500)
- Include request/response examples where helpful

## Type Generation Workflow

### Backend Generation (Go)
- **Tool:** `oapi-codegen`
- **Config:** `internal/api/oapi-codegen.yaml`
- **Output:** `internal/api/server.gen.go`
- **Command:** Run `go generate ./internal/api` or use Makefile target
- **Pattern:** Generate server interface, then implement handlers

### Frontend Generation (TypeScript)
- **Tool:** `@hey-api/openapi-ts`
- **Output:** Entire directory `frontend/src/lib/api/` (e.g. `index.ts`, `sdk.gen.ts`, `types.gen.ts`, `zod.gen.ts`, `client.gen.ts`, `core/`, `client/`, `@tanstack/`)
- **Command:** `npm run generate:api` (in frontend directory)
- **Usage:** Import SDK functions and types from `@/lib/api`; Zod schemas from `@/lib/api/zod.gen`

## Type Mapping Patterns

### Backend Handler Pattern
```go
// Generated type from OpenAPI
type CreateDeviceRequest struct {
    Name string `json:"name"`
}

// Handler extracts values and maps to domain
func (h *Handler) CreateDevice(w http.ResponseWriter, r *http.Request) {
    var req api.CreateDeviceRequest
    if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
        // handle error
    }
    
    // Map to domain model (extract primitives)
    device, err := h.service.CreateDevice(r.Context(), req.Name)
    // ...
}
```

### Frontend Client Pattern
```typescript
import { createDevice, getDevices } from '@/lib/api';
import type { CreateDeviceRequest, Device } from '@/lib/api';
import { toApiError, toErrorMessage } from '@/lib/api-client';

// SDK functions are generated from operationId
const response = await createDevice({
  body: { name: 'Device Name' } as CreateDeviceRequest,
  throwOnError: false,
});
if (response.error) throw toApiError(response.error);
const data = response.data; // typed as Device

const listResponse = await getDevices({ throwOnError: false });
if (listResponse.error) throw toApiError(listResponse.error);
const devices = listResponse.data ?? [];
```

## API Client Implementation

### Backend
- Use generated server interface from `oapi-codegen`
- Implement handlers matching the interface
- Use `kin-openapi` middleware for request validation
- Map OpenAPI types to domain types in handlers

### Frontend
- Use generated SDK functions from `@/lib/api` (e.g. `getDevices`, `createDevice`)
- Use `@/lib/api-client` for config, `toApiError`, `toErrorMessage`, and `queryKeys`
- All API calls go through the generated SDK; never write manual fetch calls
- Types and Zod schemas come from `@/lib/api` and `@/lib/api/zod.gen`

## Synchronization Rules

### When to Regenerate
1. **After API changes:** Always regenerate types after modifying `api/openapi.yaml`
2. **Before backend changes:** Regenerate to ensure handlers match spec
3. **Before frontend changes:** Regenerate to get latest types

### Regeneration Commands
- **Backend:** `go generate ./internal/api` or check Makefile
- **Frontend:** `cd frontend && npm run generate:api`

### Validation
- Backend: Use `kin-openapi` middleware to validate requests match spec
- Frontend: TypeScript compiler validates types at build time
- Both: Ensure generated types are committed to version control

## Error Response Patterns

### Standard Error Schema
```yaml
ErrorResponse:
  type: object
  properties:
    error:
      type: string
      description: Human-readable error message
    code:
      type: string
      description: Machine-readable error code
```

### HTTP Status Codes
- `200`: Success
- `201`: Created
- `400`: Bad Request (validation errors)
- `401`: Unauthorized (authentication required)
- `403`: Forbidden (insufficient permissions)
- `404`: Not Found
- `409`: Conflict (e.g., duplicate username)
- `500`: Internal Server Error

## Security Definitions

### Cookie Authentication
- Used for session-based auth
- HTTP-only, secure cookies
- Defined in OpenAPI security schemes

### Bearer Authentication
- Used for token-based auth (if needed)
- Defined in OpenAPI security schemes

### Security Application
- Global security requirement denies access by default
- Public endpoints explicitly set `security: []`
- Protected endpoints inherit global security

## Best Practices

1. **Keep Spec Updated:** Update `api/openapi.yaml` first, then regenerate types
2. **Version Control:** Commit generated types to ensure consistency
3. **Documentation:** Include descriptions in OpenAPI spec for better IDE hints
4. **Validation:** Use schema validation in both backend and frontend
5. **Breaking Changes:** Update version number in OpenAPI info when making breaking changes
6. **Examples:** Include request/response examples in spec for documentation
